# PaperResearchAgent

Агент для поиска и саммаризации научных статей с использованием LangGraph и LangChain.

## Описание

`PaperResearchAgent` - это агент, который автоматически ищет научные статьи в arXiv и создает их краткие резюме с помощью LLM. Агент использует LangGraph для оркестрации процесса поиска и саммаризации.

## Возможности

- 🔍 Поиск статей в arXiv
- 📝 Саммаризация найденных статей с помощью LLM
- 🔄 Оркестрация процесса с использованием LangGraph
- 📊 Возврат структурированных результатов с метаданными статей

## Архитектура

Агент использует LangGraph для управления состоянием процесса:

1. **Поиск статей** (`search_papers` node):
   - Поиск в arXiv по запросу

2. **Саммаризация статей** (`summarize_papers` node):
   - Для каждой найденной статьи создается резюме с помощью LLM
   - Сохранение метаданных (название, авторы, URL, источник)

## Использование

### Базовый пример

```python
from mas_automl.agents import PaperResearchAgent

# Инициализация агента
agent = PaperResearchAgent()

# Обработка запроса (можно передать строку напрямую)
state = await agent.handle("AutoML machine learning")

# Просмотр результатов
if state.get('error'):
    print(f"Ошибка: {state['error']}")
else:
    summaries = state.get("summaries", [])
    for summary in summaries:
        print(f"Название: {summary['title']}")
        print(f"Резюме: {summary['summary']}")
        print(f"URL: {summary['url']}")
```

### Структура ответа

Агент возвращает `PaperSearchState` (TypedDict) напрямую:

```python
{
    "query": "запрос пользователя",
    "papers": [...],  # список найденных статей (сырые данные)
    "summaries": [    # список саммаризированных статей
        {
            "title": "Название статьи",
            "authors": ["Автор 1", "Автор 2"],
            "abstract": "Оригинальный абстракт",
            "summary": "Резюме на русском языке",
            "url": "URL статьи",
            "source": "arxiv",
            "published_date": "дата публикации"
        },
        ...
    ],
    "current_paper_index": 0,
    "error": None  # или строка с ошибкой, если произошла ошибка
}
```

## Настройка

Агент использует настройки из `mas_automl.config.settings`:

- `settings.llm.model` - модель LLM для саммаризации
- `settings.llm.temperature` - температура для генерации
- `settings.llm.api_key` - API ключ для LLM

## Зависимости

- `langchain` - для работы с LLM
- `langchain-openai` - для интеграции с OpenAI
- `langgraph` - для оркестрации процесса
- `arxiv` - для поиска в arXiv

## Примеры использования

См. `experiments/notebooks/paper_research_example.ipynb` для подробных примеров.
