# AutoML Benchmark – используемые датасеты по режимам

Этот документ описывает, какие датасеты используются в разных режимах выполнения **AutoML Benchmark (AMLB)**.

AMLB не хранит собственные данные — он использует датасеты из **OpenML**. Ниже приведён сводный список OpenML ID по каждому режиму.

---

## 📌 Режим: **test**

Небольшой, быстрый режим проверки корректности пайплайна и интеграций.

| OpenML ID | Название |
| --------- | -------- |
| **3**     | kr-vs-kp |
| **31**    | credit-g |
| **37**    | diabetes |

---

## 🟦 Режим: **medium**

Используется для более серьёзных сравнений; включает средние по размеру и структуре датасеты.

| OpenML ID | Название       |
| --------- | -------------- |
| **31**    | credit-g       |
| **37**    | diabetes       |
| **4134**  | Bioresponse    |
| **40983** | covertype      |
| **40966** | airlines       |
| **4534**  | phoneme        |
| **41150** | australian     |
| **1485**  | madelon        |
| **9952**  | bank-marketing |

---

## 🟥 Режим: **benchmark**

Полная выборка датасетов, используемая в основном в научных публикациях AMLB.

| OpenML ID | Название          |
| --------- | ----------------- |
| **3**     | kr-vs-kp          |
| **12**    | mfeat-factors     |
| **14**    | mfeat-fourier     |
| **31**    | credit-g          |
| **37**    | diabetes          |
| **44**    | spambase          |
| **179**   | adult             |
| **293**   | cpu-small         |
| **1049**  | wilt              |
| **4134**  | Bioresponse       |
| **40983** | covertype         |
| **40966** | airlines          |
| **41142** | vehicle           |
| **41150** | australian        |
| **9952**  | bank-marketing    |
| **1485**  | madelon           |
| **1464**  | blood-transfusion |
| **1479**  | hill-valley       |
| **1596**  | ozone-level-8hr   |

---

## 📥 Как использовать модуль для работы с датасетами

Модуль `mas_automl.datasets` предоставляет удобный интерфейс для работы с датасетами из OpenML:

```python
from mas_automl.datasets import DatasetManager

manager = DatasetManager()

# Получить метаданные (без скачивания данных)
manager.get_openml_dataset(31, download=False)
metadata = manager.metadata  # или manager.get_metadata()

# Загрузить данные датасета
df = manager.get_openml_dataset(31, download=True)

# Получить метаданные по имени
manager.get_openml_dataset("diabetes", download=False)
metadata = manager.metadata
```

---

## 📊 Метаданные датасетов из OpenML

При загрузке датасета из OpenML модуль `mas_automl.datasets` автоматически получает полные метаданные, которые включают:

### Основные идентификаторы

- **`dataset_id`** — уникальный OpenML ID датасета
- **`name`** — название датасета
- **`version`** — версия датасета
- **`version_label`** — метка версии (если есть)

### Описание и источники

- **`description`** — подробное описание датасета
- **`citation`** — информация для цитирования
- **`paper_url`** — URL публикации, связанной с датасетом
- **`paper_reference`** — библиографическая ссылка
- **`creator`** — создатель датасета
- **`contributor`** — вкладчик
- **`collection_date`** — дата сбора данных
- **`upload_date`** — дата загрузки в OpenML

### Локализация и лицензия

- **`language`** — язык данных
- **`licence`** — лицензия использования

### Структура данных

- **`format`** — формат данных (ARFF, CSV и т.д.)
- **`file_id`** — ID файла в OpenML
- **`default_target_attribute`** — целевая переменная по умолчанию
- **`ignore_attribute`** — список атрибутов для игнорирования
- **`row_id_attribute`** — атрибут ID строки

### Статистика

- **`num_rows`** — количество строк (экземпляров)
- **`num_features`** — количество признаков
- **`num_classes`** — количество классов (для классификации)
- **`num_missing_values`** — количество пропущенных значений

### Классификация

- **`dataset_type`** — тип задачи ML (`classification`, `regression`, `time_series`, `unknown`)
- **`tags`** — теги для категоризации
- **`status`** — статус датасета
- **`visibility`** — видимость датасета

### Пример использования

```python
from mas_automl.datasets import DatasetManager

manager = DatasetManager()

# Загружаем метаданные (данные не скачиваются)
manager.get_openml_dataset(31, download=False)

# Получаем метаданные через атрибут или метод
metadata = manager.metadata  # или metadata = manager.get_metadata()

print(f"Название: {metadata.name}")
print(f"Описание: {metadata.description}")
print(f"Количество строк: {metadata.num_rows}")
print(f"Количество признаков: {metadata.num_features}")
print(f"Тип задачи: {metadata.dataset_type}")
print(f"Целевая переменная: {metadata.default_target_attribute}")

# Или получить метаданные напрямую по ID/имени
metadata = manager.get_dataset_info(31)  # или manager.get_dataset_info("credit-g")
```

---

## 🔍 Метрики качества (Dataset Qualities)

OpenML автоматически вычисляет метрики качества для каждого датасета при его загрузке на платформу. Эти метрики помогают быстро оценить характеристики данных и их пригодность для машинного обучения.

### Как получаются метрики качества

Метрики качества вычисляются автоматически сервером OpenML при загрузке датасета. Процесс включает:

1. **Автоматический анализ данных** — OpenML использует инструменты из экосистемы Weka для анализа структуры данных
2. **Вычисление статистических характеристик** — базовые метрики (количество строк, признаков, пропущенных значений и т.д.)
3. **Оценка сложности датасета** — запуск простых моделей для оценки сложности задачи
4. **Сохранение результатов** — все метрики сохраняются в атрибуте `qualities` датасета

### Основные типы метрик

#### 1. Статистические метрики

- **`NumberOfInstances`** — количество экземпляров (строк)
- **`NumberOfFeatures`** — количество признаков
- **`NumberOfClasses`** — количество классов (для классификации)
- **`NumberOfMissingValues`** — количество пропущенных значений
- **`NumberOfNumericFeatures`** — количество числовых признаков
- **`NumberOfSymbolicFeatures`** — количество категориальных признаков

#### 2. Метрики сложности данных

- **`AutoCorrelation`** — средняя автокорреляция признаков
  - Показывает, насколько признаки коррелируют друг с другом
  - Диапазон: 0–1 (ближе к 1 — высокая корреляция)

#### 3. Метрики производительности моделей (CfsSubsetEval)

Эти метрики вычисляются с использованием метода **Correlation-based Feature Selection (CFS)** из Weka:

1. **Отбор признаков** — CFS выбирает подмножество признаков на основе корреляций между признаками и целевой переменной
2. **Оценка на простых моделях** — на отобранных признаках запускаются простые модели:
   - **DecisionStump** — дерево решений глубины 1 (stump)
   - **NaiveBayes** — наивный байесовский классификатор
   - **kNN1N** — k-ближайших соседей (k=1)

3. **Вычисление метрик** — для каждой модели вычисляются:
   - **AUC** (Area Under ROC Curve) — площадь под ROC-кривой
     - Диапазон: 0–1
     - 1.0 — идеально, 0.5 — случайное угадывание
   - **ErrRate** (Error Rate) — доля ошибок классификации
     - Диапазон: 0–1
     - 0.0 — идеально, 1.0 — все предсказания неверны
   - **Kappa** (Cohen's Kappa) — коэффициент согласованности
     - Диапазон: -1 до 1
     - >0.6 — хорошее согласие, >0.8 — отличное

### Пример метрик качества

```python
from mas_automl.datasets import DatasetManager

manager = DatasetManager()

# Загружаем метаданные
manager.get_openml_dataset(31, download=False)
metadata = manager.metadata

# Доступ к метрикам качества
if metadata.quality:
    print("Метрики качества:")
    for key, value in metadata.quality.items():
        print(f"  {key}: {value}")
```

Пример вывода:

```text
Метрики качества:
  AutoCorrelation: 0.5695695695695696
  CfsSubsetEval_DecisionStumpAUC: 0.7208285714285714
  CfsSubsetEval_DecisionStumpErrRate: 0.273
  CfsSubsetEval_DecisionStumpKappa: 0.3056968463886062
  CfsSubsetEval_NaiveBayesAUC: 0.7208285714285714
  CfsSubsetEval_NaiveBayesErrRate: 0.273
  CfsSubsetEval_NaiveBayesKappa: 0.3056968463886062
  CfsSubsetEval_kNN1NAUC: 0.7208285714285714
  CfsSubsetEval_kNN1NErrRate: 0.273
  CfsSubsetEval_kNN1NKappa: 0.3056968463886062
```

### Зачем нужны метрики качества?

Метрики качества помогают:

- ✅ **Быстро оценить сложность датасета** — понять, насколько сложна задача
- ✅ **Сравнить датасеты** — выбрать подходящие датасеты для экспериментов
- ✅ **Выбрать алгоритмы** — понять, какие модели могут хорошо работать
- ✅ **Оценить качество признаков** — понять, насколько информативны признаки после отбора

### Дополнительная информация

Для получения подробной информации о конкретных метриках рекомендуется:

1. Посетить страницу датасета на OpenML: `https://www.openml.org/d/{dataset_id}`
2. Изучить документацию Weka по CfsSubsetEval
3. Обратиться к исходному коду OpenML (если доступен)

---
